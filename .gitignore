# macOS
.DS_Store

# Node (not used in production, but local tooling only)
node_modules/

# Editor
.vscode/
.idea/
*.swp

# Env / local
.env
.env.local
*.local

# Build artifacts (none expected, but just in case)
dist/
